interface:
  display_name: "RenderDoc MCP"
  short_description: "Analyze and reconstruct RenderDoc GPU captures"
  default_prompt: "Use $renderdoc-mcp to inspect or reconstruct a draw from this RenderDoc capture."

policy:
  allow_implicit_invocation: true
